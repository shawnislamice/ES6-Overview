const add = (first, second) => first + second;
const multiply = (first, second) => first * second;
const dividedFirstBySecond = (first, second) => first / second;




export {add,multiply,dividedFirstBySecond};
